{% set color = color2 | blend(color3) | blend(color4) | blend(color5) | blend(color6) %}

$fg = rgb({{foreground | strip}})
$fg-alt = rgb({{color7 | strip}})
$bg = rgb({{background | strip}})
$bg-alt = rgb({{color8 | strip}})
$cursor = rgb({{cursor | strip}})
$border_active = rgb({{color | strip}})
$border_inactive = rgb({{color | blend(background) | strip}})
